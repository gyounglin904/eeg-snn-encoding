# EEG Resonate Encoding SNN 프로젝트

세종과학고 정보과학동아리 CPU 미니프로젝트. 기간 2주. 최종 산출물은 발표.

---

## 1. 연구 질문

뇌파는 진동 신호다. 진동에 공명하는 뉴런(Resonate-and-Fire)으로 스파이크 인코딩하면
기존 인코딩(Rate, Delta)보다 EEG 분류에서 유리한가?

### 스토리라인 (발표 논리)

1. 뇌파는 진동 신호다. mu(8–13 Hz), beta(13–30 Hz) 리듬에 정보가 실린다.
2. 기존 인코딩은 진동을 못 담는다. Rate는 진폭만, Delta는 변화량만 스파이크로 바꾼다.
3. Resonate 뉴런은 진동에 공명한다. 주파수 분해와 스파이크 생성을 동시에 한다.
4. 따라서 EEG에는 Resonate 인코딩이 유리할 것이다.
5. 검증: 정확도와 스파이크 수(에너지)에서 우수한가?
6. 추가 검증: 공진 주파수를 mu·beta에 맞췄을 때가 어긋나게 놨을 때보다 나은가?
   → 이득의 원인이 정말 "진동과의 공명"임을 확인

6번이 없으면 4번은 가정에 머문다. 있으면 주장이 된다.

### 가설

- H1. 정확도: E4(Resonate) ≥ E3(필터뱅크) > E2(Delta) ≈ E1(Rate)
- H2. 동일 정확도에서 E4의 총 스파이크 수가 E3보다 적다
- H3. 대역 정렬 효과(정렬 − 어긋남)의 크기가 E3보다 E4에서 크다

---

## 2. 독창성의 범위 (과장 금지)

### 기존 연구인 것 — 새롭다고 주장하면 안 됨
- Resonate-and-Fire 뉴런 (Izhikevich 2001)
- RF를 입력 인코더로 사용 (Auge et al., 오디오·키워드 검출)
- RF의 희소성·에너지 이점
- EEG를 SNN으로 분류
- EEG에 공진 소자 적용 (SCTN 기반, 2024)

### 이 프로젝트의 새로운 부분 — 하나뿐
동일 출력 차원의 통제군(E3)을 두어 **주파수 분해의 기여와 공진 동역학의 기여를 분리 측정**한다.
여기에 공진 주파수 배치 절제(정렬/균등/어긋남)를 겹쳐 대역 정렬 효과를 정량화한다.

### 중요한 이론적 사실
RF 뉴런의 선형 부분은 지수 윈도우 STFT와 수학적으로 동등하다
(z[t] = λe^{iωΔt}z[t-1] + a[t] → z[t] = Σₙ e^{inωΔt}λⁿa[t−n]).
따라서 E4 − E3 차이는 선형 부분이 아니라 **스파이크 생성 규칙과 리셋(비선형 부분)**에서만 나온다.
차이가 작을 가능성이 높으므로, 작은 효과를 검출할 수 있게 설계해야 한다.

### 발표에서 쓸 표현 / 쓰면 안 되는 표현
- 금지: "RF를 EEG에 처음 적용했다" / "새로운 인코딩을 제안했다" / "SOTA 성능"
- 사용: "선행 연구는 RF를 FFT 대체 수단 또는 내부 뉴런 모델로 다뤘기 때문에,
  입력 인코더로서 동일 차원 통제군과 비교한 사례가 드물다. 우리는 그 조건에서 측정했다."

---

## 3. 실험 설계

### 인코더 4종 — 출력 shape를 (T, B, C=9, N=8)로 전부 통일

| ID | 인코더 | 8차원이 나뉘는 기준 |
|----|--------|---------------------|
| E1 | Rate + 가우시안 수용장(GRF) | 진폭 구간 8개 |
| E2 | 다중임계 Delta | 변화량 임계값 8단계 |
| E3 | 지수윈도우 STFT 필터뱅크 + Delta | 중심주파수 8개 (통제군) |
| E4 | **Resonate 공진 인코딩** | 공진주파수 8개 (제안) |

차원 통일 이유: 차원이 다르면 Conv 층 파라미터 수가 달라져 "인코딩만 바꿨다"가 깨진다.
E1, E2가 표준형이 아닌 변형이라는 점은 한계 슬라이드에 명시한다.

E3는 임의의 Butterworth가 아니라 **RF와 동일한 지수 윈도우 STFT**로 구현한다.
그래야 E3와 E4의 선형 부분이 문자 그대로 같아지고, 차이가 스파이크 생성 규칙 하나로 좁혀진다.

### 대역 배치 절제 (E3, E4에 적용)

| 조건 | 공진주파수 8개 위치 | 의도 |
|------|---------------------|------|
| 정렬 | 8–30 Hz 로그 배치 | mu·beta에 맞춤 |
| 균등 | 2–45 Hz 균등 | 사전지식 없음 |
| 어긋남 | 32–45 Hz | mu·beta 회피 — 성능이 떨어져야 정상 |

어긋남을 36–60 Hz가 아닌 32–45 Hz로 잡은 이유: PhysioNet 데이터는 미국 측정이라
60 Hz 전원 노이즈가 있다. 어긋남 조건만 노이즈를 받으면 원인 해석이 불가능해진다.

### 실행 조건
E1, E2, E3-정렬, E4-정렬, E4-균등, E4-어긋남 (6조건)
× 피험자 5명 (여유 있으면 10명) × 시드 2개 = 60회

---

## 4. 데이터

- 데이터셋: PhysioNet EEG Motor Movement/Imagery (`mne.datasets.eegbci`)
- runs = [4, 8, 12] (왼손/오른손 주먹 쥐기 심상)
- 과제: 2클래스 (왼손 vs 오른손)
- 채널 9개: FC3, FCz, FC4, C3, Cz, C4, CP3, CPz, CP4
- 전처리: 1–45 Hz 대역통과 → 128 Hz 리샘플 → 채널별 z-score
- 에폭: 큐 후 0.5–2.0 s → T = 192 스텝
- 분할: 피험자 내 학습/검증

### 왜 이 채널·이 대역인가
운동심상은 ERD(event-related desynchronization)로 나타난다.
왼손 심상 → 오른쪽 운동피질(C4)의 mu·beta 감소, 오른손 심상 → C3 감소.
분류기는 이 좌우 비대칭을 읽는다.

---

## 5. 네트워크 (모든 조건 공통, 절대 변경 금지)

```python
net = nn.Sequential(
    nn.Conv2d(1, 12, 3, padding=1),
    snn.Leaky(beta=beta, spike_grad=spike_grad, init_hidden=True),
    nn.MaxPool2d(2),
    nn.Conv2d(12, 32, 3, padding=1),
    snn.Leaky(beta=beta, spike_grad=spike_grad, init_hidden=True),
    nn.Flatten(),
    nn.Linear(32 * h * w, 2),
    snn.Leaky(beta=beta, spike_grad=spike_grad, init_hidden=True, output=True),
)
```

- 8장 CSNN 구조 기반. 커널은 5×5가 아니라 3×3 (입력 맵이 9×8로 작음)
- 8장과 달리 매 스텝 **다른** 스파이크를 넣는다 (`spk_in[step]`).
  8장은 같은 MNIST 이미지를 25번 반복했지만 여기서는 시간축에 실제 정보가 있다.
- 출력층은 클래스당 뉴런 1개(총 2개)로 고정. 12장 population coding은 쓰지 않는다.
- 인코더 파라미터는 학습시키지 않는다. 학습시키면 E3/E4 차이가 인코더 표현력이 아니라
  최적화 운의 차이로 오염된다.
- 가중치 초기화 시드, 에폭 수, 학습률, 배치 크기, 데이터 분할 전부 동일.
  **바뀌는 것은 forward_pass에 들어가는 spk_in 하나뿐.**

---

## 6. 코드 규칙 (쿡북 스타일 — 반드시 준수)

첨부된 SNN 쿡북 노트북 16개의 형식을 그대로 따른다.

- **if 문과 try/except를 쓰지 않는다.** 조건 판정은 불리언 산술로 한다.
  예: 상향 영교차 = `(v <= 0).float() * (v_next > 0).float()`
- 주석은 한국어. 코드 오른쪽에 `#`로 짧게.
- `import koreanize_matplotlib`, `torch.manual_seed(0)`
- 변수명: `spk_in`, `spk_rec`, `mem_rec`, `num_steps`, 기록은 `_rec` 접미사
- 모듈 수준 상수는 대문자 (`FS`, `NUM_STEPS`, `BETA`)
- 노트북 맨 앞에 "학습 목표", 맨 뒤에 "정리" 절
- 절 번호 매기기: "1. 설치와 임포트", "2. ..." 형식
- 상태 갱신은 명시적 for 루프로 전개 (6장 snn.Synaptic 루프와 동일 형태)
- matplotlib: `figsize` 지정, `tight_layout()`, tab: 계열 색상

### Resonate 인코더 참조 구현

```python
def encode_resonate(signal, freqs, fs, decay=0.98, threshold=0.15):
    # signal: (배치, 채널, 시간) / freqs: (F,) 공진 주파수 [Hz]
    omega = 2 * torch.pi * freqs / fs          # 스텝당 위상 증가량
    cos_w, sin_w = torch.cos(omega), torch.sin(omega)
    u = torch.zeros(signal.size(0), freqs.numel(), signal.size(1))   # 실수부
    v = torch.zeros_like(u)                                          # 허수부
    spk_rec = []
    for step in range(signal.size(2)):
        x = signal[:, None, :, step]                                 # (배치,1,채널)
        u_next = decay * (cos_w[:, None] * u - sin_w[:, None] * v) + x
        v_next = decay * (sin_w[:, None] * u + cos_w[:, None] * v)
        spk = (v <= 0).float() * (v_next > 0).float() \
              * (u_next > threshold).float()   # 허수부 상향 영교차 + 실수부 임계 초과
        u, v = u_next, v_next
        spk_rec.append(spk)
    return torch.stack(spk_rec)                # (시간, 배치, F, 채널)
```

---

## 7. 노트북 구성

각 노트북은 앞 노트북의 출력을 파일로 받는다.
한 노트북에 다 넣으면 뒤에서 오류 날 때마다 데이터 로딩부터 다시 해야 한다.

### notebooks/01_data_preparation.ipynb
PhysioNet 다운로드 → 전처리 → 에폭 → z-score → `.npz` 저장
확인: 피험자당 약 45시행, 두 클래스 개수가 비슷할 것

### notebooks/02_sanity_check.ipynb
**SNN 코드를 한 줄도 짜기 전에 실행한다.**
mu·beta 대역 전력(9채널 × 2대역 = 18차원) → 로지스틱 회귀 5-fold CV
**통과 기준: 피험자 평균 65% 이상 (우연은 50%)**
미달이면 전처리·채널 선택 문제이지 SNN 문제가 아니다. 통과 못 하면 다음으로 넘어가지 않는다.

추가 그림: C3 채널의 주파수별 대역 전력을 왼손/오른손으로 나눠 그린다.
8–30 Hz에서 벌어지고 32 Hz 위에서 겹치는 그림이 나오면 대역 조건 설계의 근거가 된다.

### notebooks/03_encoders.ipynb
E1~E4 구현 + 래스터 플롯 비교 + 스파이크 수 집계
합성 사인파로 먼저 검증 가능 (EEG 없이도 됨)
확인: Resonate 뉴런의 내부 상태가 진동하고 공진 주파수 근처에서 진폭이 커질 것.
스파이크 수가 거의 0이거나 과도하면 threshold 조정.

### notebooks/04_train_compare.ipynb
6조건 × 5피험자 × 2시드 = 60회 학습, 결과를 CSV로 기록
회당 2–3분, 총 3시간. **코랩 GPU 런타임 필수.**
8장 쿡북 학습 루프는 1 에폭 부분 데이터라 그대로 쓰면 안 된다. 다중 에폭 + 검증 분할로 확장.

### notebooks/05_analysis.ipynb
조건별 정확도 평균±표준편차 / 정확도–스파이크 수 산점도 /
대응표본 Wilcoxon 검정(E4 vs E3) / 대역 정렬 3조건 비교

---

## 8. 평가 지표

1. 검증 정확도 (피험자별 평균 ± 표준편차)
2. 추론 1회당 총 스파이크 수
3. 에너지 프록시: SNN 누적연산(AC) 0.9 pJ, ANN MAC 4.6 pJ (45 nm CMOS)
4. 정확도–스파이크 수 트레이드오프 산점도 ← 발표 핵심 그림
5. 같은 피험자·같은 분할이므로 대응표본 검정 사용

---

## 9. 알려진 위험

1. **모든 인코더가 우연 수준(50%)에 머무르면 비교가 무의미해진다.**
   → 노트북 2를 반드시 먼저 통과시킬 것.
2. E4 − E3 차이가 작을 것이다 (선형 부분이 등가이므로).
   → 피험자 수를 늘리고, 정확도뿐 아니라 스파이크 수도 볼 것.
3. 인코더 하이퍼파라미터(threshold, decay)를 조건마다 공평하게 탐색하지 않으면 결과가 오염된다.
   각 조건에 동일한 탐색 예산을 준다.
4. 정확도 절대값이 낮을 것이다. 비교가 목적임을 명시하지 않으면 그 점만 지적받는다.
5. 피험자 5명·시드 2개는 통계적으로 빈약하다. 시간이 남으면 피험자를 늘리는 것이 최선의 개선.

---

## 10. 일정

| 일차 | 작업 |
|------|------|
| 1–2 | 노트북 1, 2 |
| 3–5 | 노트북 3 |
| 6–9 | 노트북 4 (코랩 GPU) |
| 10–11 | 노트북 5 |
| 12–14 | 발표자료 |

## 11. 발표 구성

1. 뇌파는 진동이다 (동기)
2. 기존 인코딩은 진동을 버린다 (문제)
3. Resonate 뉴런 — 공명하며 스파이크 (방법)
4. 실험 설계 (통제 설명)
5. 결과: 정확도·스파이크 수 비교 (주 결과)
6. 대역 정렬 절제 (원인 확인)
7. 추가: 필터뱅크와의 비교 (심화)
8. 한계와 결론
